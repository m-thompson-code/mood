import { ExcitedPipe } from './excited.pipe';

describe('ExcitedPipe', () => {
    it('create an instance', () => {
        const pipe = new ExcitedPipe();
        expect(pipe).toBeTruthy();
    });
});
